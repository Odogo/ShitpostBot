import { Guild, VoiceChannel } from "discord.js";
import { MMusicPlayer, MusicTextBasedChannel } from "../database/MMusicPlayer";
import { MMusicQueue } from "../database/MMusicQueue";

import provider from "play-dl";

/**
 * This class is the manager for handling connections to the database through {@link MMusicPlayer} and {@link MMusicQueue}
 * @author Kyomi
 */
export class Media {

    
    
}